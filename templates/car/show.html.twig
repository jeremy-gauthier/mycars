{% extends 'base.html.twig' %}

{% block title %}voiture{% endblock %}

{% block body %}
    <h1>Voiture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ car.id }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ car.model }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ car.year }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('car_index') }}">retourner à la liste des voitures</a>

    <a href="{{ path('car_edit', {'id': car.id}) }}">Modifier</a>

    {{ include('car/_delete_form.html.twig') }}
{% endblock %}
